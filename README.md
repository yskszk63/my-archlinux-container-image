# my-archlinux-container-image

Container image for me.

## Usage

```bash
podman run --rm -it ghcr.io/yskszk63/my-archlinux-container-image:latest
```

or

```bash
docker run --rm -it ghcr.io/yskszk63/my-archlinux-container-image:latest
```
